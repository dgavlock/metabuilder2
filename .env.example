# MetaBuilder - Environment Variables
# Copy this file to .env.local and fill in your values.
#
# Both keys are optional. Users can provide their own API keys
# in the Settings panel inside the app.
# Server-side keys act as a shared fallback for all users.

# Anthropic (Claude) API key
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (GPT) API key
# OPENAI_API_KEY=sk-...
